spring:
  datasource:
    url: jdbc:h2:mem:pizzashop-localdev;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""

pizzashop:
  admin:
    # Seeds the allowlist so /dev-login has an admin to sign in as.
    bootstrap-email: ${ADMIN_BOOTSTRAP_EMAIL:dev-admin@pizzashop.local}
